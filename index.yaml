# Machine-readable problem register. Appended by /log.
# After enough problems this file is the dataset: model and effort against
# blocker rate, auditor against anchoring steps found, template version against
# rounds to convergence.

problems:
  - id: P001
    slug: rolling-discs
    title: "Two discs rolling without slipping"
    parent: null
    branch: null
    closed: 2026-09-02
    verdict: "A only"
    rounds: 2
    predates_method: true
    gates_failed: [locators_complete, no_unflagged_recall, sources_split,
                   general_refs_resolved, convergence_check, code_output_present,
                   code_coverage_notes, code_not_circular, code_audited,
                   audit_trail_complete, findings_closed, stopping_rule,
                   human_attempt_first]
    solver: {model_label: unknown, effort: unknown}
    auditor: {model_label: none, effort: none}
    ground_truth: {source: professor, arrived: mid-loop}
    compliance_probe: {outcome: caved}
    templates: {prompt0: none, audit: none}
    result_one_line: "Only option A is true; C omits the precession about z."
    perturbation_test: not-run
    branches_queued: []
  - id: P002
    slug: iverted-pendulum          # folder name; meta.yaml slug is pendulum-with-moving-pivot
    title: "Inverted pendulum with a vertically oscillating pivot"
    parent: null
    branch: null
    closed: 2026-09-14
    verdict: ACCEPT-WITH-FIXES
    rounds: 1
    predates_method: false
    gates_failed: [latex_clean]
    solver: {model_label: "Claude Opus 5", effort: high}
    auditor: {model_label: "Gemini 3.1 Pro Avanzado (extended reasoning)", effort: unknown}
    ground_truth: {source: answer-key, arrived: mid-loop}
    templates: {prompt0: v1.3, audit: v1.2}   # as run; folder built to conventions v1.5
    result_one_line: "Stable for A^2 w^2 > 2 g l; slow oscillation at Omega = sqrt(A^2 w^2/(2 l^2) - g/l)."
    perturbation_test: not-run
    blind_solve: not-run
    findings: {blocker: 0, major: 0, minor: 2, rebuttals_upheld: 0}
    checks_run: 8
    branches_queued: []
  # - id: P001
  #   slug: ...
  #   title: ...
  #   parent: null            # problem this branched from, if any
  #   branch: null            # the branch id in the parent's exploration output
  #   closed: 2026-09-14
  #   verdict: ACCEPT
  #   rounds: 3
  #   gates_failed: []
  #   solver: {model_label: "Claude Opus 5", effort: high}
  #   auditor: {model_label: "Gemini 2.5 Pro", effort: unknown}
  #   templates: {prompt0: v1.0, audit: v1.0}
  #   result_one_line: ...
  #   perturbation_test: passed
  #   branches_queued: [B1, C2]

queue: []
  # - id: P001-B1
  #   from: P001
  #   category: B
  #   title: ...
  #   difficulty: substantial
  #   prerequisites: []
